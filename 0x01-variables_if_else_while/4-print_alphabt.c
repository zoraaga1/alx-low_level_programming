#include <stdio.h>
/**
 * main - print alphabets
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; ++letter)
	{
	if (letter == e)
	{
		continue;
	}
	if (letter == q)
        {
		continue;
        }
	{
		putchar(letter);
	}
	}
	
	putchar('\n');
	return (0);
}
