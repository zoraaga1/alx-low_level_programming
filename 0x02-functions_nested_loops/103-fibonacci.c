#include <stdio.h>

/**
 * main - print the sum of fibonacci numbers
 * not exceeding 4,000,000.
 * Return: 0
 */
int main(void)
{
	int a = 1, b = 2;
	int sum = 0;
	int c;

	while (b < 4000000)
	{
		if (b % 2 == 0)
			total += b;

		c = b;
		b += i;
		a = c;
	}
	printf("%d\n", sum);
	return (0);
}
